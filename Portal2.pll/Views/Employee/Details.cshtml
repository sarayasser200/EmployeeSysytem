@model Portal2.BLL.ModelVM.EmployeeVM.GetEmployeeDetailsVM

@{
    ViewData["Title"] = "Employee Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="card">
    <div class="card-header">
        Employee Details
    </div>
    <div class="card-body">
        <img src="@Url.Content("~/imageEmployee/Profile/" + Model.Image)" class="card-img" alt="Employee Photo">
        <h5 class="card-title">@Model.Name</h5>
        <p class="card-text">Age: @Model.Age</p>
        <p class="card-text">Status: @(Model.IsDeleted ? "Deleted" : "Active")</p>
        <a href="@Url.Action("Index", "Employee")" class="btn btn-back">Back to List</a>
    </div>
</div>
<style>
    .card {
        width: 100%;
        max-width: 600px;
        margin: 20px auto;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0px 0px 25px rgba(0, 0, 0, 0.3);
        background: #f9f9f9;
    }

    .card-header {
        background-color: #003366; 
        color: #ffffff; 
        padding: 15px;
        text-align: center;
        font-size: 1.5rem;
        font-weight: bold;
    }

    .card-body {
        padding: 20px;
        text-align: center;
    }

    .card-img {
        width: 100%;
        max-height: 300px;
        object-fit: cover;
        border-bottom: 5px solid #003366;
        border-radius: 10px;
    }

    .card-title {
        font-size: 1.75rem;
        color: #003366;
        margin-top: 15px;
        margin-bottom: 10px;
    }

    .card-text {
        font-size: 1.25rem;
        color: #333;
    }

    .btn-back {
        display: inline-block;
        padding: 10px 20px;
        background-color:
